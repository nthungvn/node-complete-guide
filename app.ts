// const text = 'This is a text will be written to a file.';

// const encoder = new TextEncoder();
// const data = encoder.encode(text);

// Deno.writeFile('dist/file.txt', data).then(() => {
//   console.log('Wrote to file');
// });
